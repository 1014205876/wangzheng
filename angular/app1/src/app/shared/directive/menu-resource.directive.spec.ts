import { MenuResourceDirective } from './menu-resource.directive';

describe('MenuResourceDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuResourceDirective();
    expect(directive).toBeTruthy();
  });
});
